FIFO script for notifications
get urxvtd working on ken-th
make .profile & 'alias' script for bash/zsh?
research configuring in ~/etc for root, +copy.sh
set XDG_CONFIG_HOME, XDG_DATA_HOME to $HOME/.config
